# Peer deps (cordis / schemastery / dsh-host-apiproxy) are supplied by the dsh
# runtime at load time; auto-installing them locally breaks because the
# published apiproxy rc has unpublished transitive deps.
auto-install-peers=false
only-built-dependencies[]=esbuild
